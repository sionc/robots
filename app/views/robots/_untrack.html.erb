<%= form_for current_user.relationships.find_by_trackee_id(@robot.id),
             :html => { :method => :delete }, :remote => true do |f| %>
  <div class="actions"><%= f.submit "Untrack" %></div>
<% end %>